apiVersion: v1
kind: Service
metadata:
  name: mysql-portfolio
  labels:
    app: mysql-portfolio
    owner: rakha
spec:
  type: ClusterIP
  selector:
    app: mysql-portfolio
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
