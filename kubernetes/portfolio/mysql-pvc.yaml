apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-portfolio-pvc
  labels:
    app: mysql-portfolio
    owner: rakha
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
