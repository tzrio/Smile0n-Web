apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-product-pvc
  labels:
    app: mysql-product
    owner: rakha
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
