apiVersion: v1
kind: Service
metadata:
  name: auth-service
  labels:
    app: auth-service
    owner: rakha
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
