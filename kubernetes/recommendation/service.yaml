apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  labels:
    app: recommendation-service
    owner: roihan
spec:
  type: ClusterIP
  selector:
    app: recommendation-service
  ports:
    - protocol: TCP
      port: 3003
      targetPort: 3003
