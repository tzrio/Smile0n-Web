apiVersion: v1
kind: Service
metadata:
  name: order-service
  labels:
    app: order-service
    owner: roihan
spec:
  type: NodePort
  selector:
    app: order-service
  ports:
    - port: 3003
      targetPort: 3003
