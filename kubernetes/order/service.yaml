apiVersion: v1
kind: Service
metadata:
  name: order-service
  labels:
    app: order-service
    owner: roihan
spec:
  type: NodePort
  selector:
    app: order-service
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
