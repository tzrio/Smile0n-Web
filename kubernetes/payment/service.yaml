apiVersion: v1
kind: Service
metadata:
  name: payment-service
  labels:
    app: payment-service
    owner: roihan
spec:
  type: NodePort
  selector:
    app: payment-service
  ports:
    - protocol: TCP
      port: 3002
      targetPort: 3002
