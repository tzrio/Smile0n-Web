apiVersion: v1
kind: Service
metadata:
  name: mysql-payment
  labels:
    app: mysql-payment
    owner: roihan
spec:
  type: ClusterIP
  selector:
    app: mysql-payment
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
